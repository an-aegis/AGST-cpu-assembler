Private Module IdleMessageLoopBreak

    [SpecialCompilerBinding(4)]
    [Debuggable(True)]
    Public Sub IdleMessageLoopBreakpoint()
        ' If you reached this breakpoint, then you attempted to break into the debugger
        ' when there was no active code executing (but there were active forms).
        Stop
    End Sub

End Module